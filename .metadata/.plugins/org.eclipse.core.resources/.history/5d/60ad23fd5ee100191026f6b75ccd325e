package Bronze;

import java.util.*;
import java.io.*;

public class BeautyPageant {
	
	public static int row; static int cols;
	private static int[][] hide;

	public static void main(String[] args) throws IOException {
		Scanner in = new Scanner(new File("pagneant.in"));
		PrintWriter out = new PrintWriter(new File("pageant.out"));
		
		rows = in.nextInt();
		cols = in.nextInt();
		hide = new int[rows][cols];
		for (int i = 0; i < rows; i++) {
				String line = in.nextLine();
				for (int j = 0; j < cols; j++) {
					
				}
			}
		}

	}

